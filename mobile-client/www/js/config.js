// Mobile Configuration
// -----------------------
var config = {};

// web: it contains config settings related to mobile interface. Like ports.
config.web = {
	port: 9980,
	protocol: 'aware'
};