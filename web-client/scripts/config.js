// Web Configuration
// -----------------------
var config = {};

// web: it contains config settings related to web interface. Like ports.
config.web = {
	address: 'localhost',
	port: 9980,
	protocol: 'aware'
};